<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.orlandocanchola.enoe">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MaterialComponentsTheme">
        <activity android:name=".OnBoardingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PreferenciasActivity"
            android:theme="@style/ConfiguraciónConsultasActivityTheme" />
        <activity android:name=".ConsultasActivity"></activity>
        <activity android:name=".ResultadosActivity" />
        <activity
            android:name=".BuscarCarreraActivity"
            android:parentActivityName=".MainActivity"
            android:theme="@style/BuscarCarreraActivityTheme" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>